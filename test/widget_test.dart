import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic test placeholder for IPTV Player
    expect(1 + 1, equals(2));
  });
}
